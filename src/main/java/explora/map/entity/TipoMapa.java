package explora.map.entity;

/** Visibilidade dun mapa. */
public enum TipoMapa {
    /** O mapa é visible para calquera usuaria. */
    PUBLICO,
    /** O mapa só é visible para os seus membros. */
    PRIVADO,
}
