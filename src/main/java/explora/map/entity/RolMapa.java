package explora.map.entity;

/** Rol dunha usuaria dentro dun mapa colaborativo. */
public enum RolMapa {
    /** Pode visualizar o mapa e os seus contidos. */
    MEMBRO,
    /** Pode crear e editar marcadores e categorías. */
    COLABORADORA,
    /** Pode xestionar membros, roles e a configuración do mapa. */
    ADMIN_MAPA
}
